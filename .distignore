# Everything here is development tooling. None of it ships.
#
# The plugin itself has no composer dependencies and no build step: what is in
# src/ and assets/ is what runs. Only the test suite needs vendor/.

.git
.github
.gitignore
.distignore
.phpunit.cache
.phpunit.result.cache
composer.json
composer.lock
phpunit.xml
phpunit.xml.dist
phpunit-multisite.xml
phpunit-multisite.xml.dist
tests
vendor
phpcs.xml.dist
.phpcs.cache
bin
dist
.wordpress-org
